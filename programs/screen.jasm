start:
    MOV MB, 0x01 ; set the memory bank to vram
    MOV A, 0x01 ; value to write to vram
    MOV B, 0x8000 ; start address

loop:
    PUT B, A
    ADD B, 0x03

    CMP B, 0xA001
    JZ loop_end

    JMP loop

loop_end:
    HALT
