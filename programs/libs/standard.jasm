macro interrupt_enable %value
    xor z, z
    or z, %value
    lsh z, 11
    or f, z
end macro

macro tty_out %string
    ; uses port 0 for output
    ; trashes y and z
    mov z, %string ; string address
    mov y, 0x8000
end macro